/**
 * Test Suite containing Specs for awaitify library
 */
describe("Awaitify",()=>{

    /**
     * Before any spec is run, Jasmine calls this function.
     */
    beforeEach(()=>{

    });

    // Each test is described using "it" function
    it("should test if awaitify function is available",()=>{
        expect(true).toEqual(false);
    });
});